import {path} from './path';
import {Module, submodules, sampleDoc, nginx} from './constants';

export {path, Module, submodules, sampleDoc, nginx};
export default {path, list: submodules, sampleDoc, nginx};